---
description: Handle an inbound lead/sample/quote request via the inbound-rfq agent
argument-hint: <lead context, e.g. pasted inbound message or sample request detail>
---
Use the inbound-rfq agent to qualify and draft a reply for this inbound lead: $ARGUMENTS

Follow the RFQ playbook's default inbound flow and log/update the pipeline tracker as instructed in that agent's definition.
